import TimeLine from "@/components/TimeLine/TimeLine";

export const metadata = {
  title: 'Timeline | KeenKeeper',
  description: 'View your activity and connection history over time.',
};

const TimeLinePage = () => {
  return <TimeLine />;
};

export default TimeLinePage;
